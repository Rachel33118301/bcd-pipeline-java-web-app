pipeline {
  // agent { label 'macos' }
  agent any
  
//   options {
//     buildDiscarder(logRotator(numToKeepStr: '5'))
//   }
  stages {
    stage('Tooling versions') {
      steps {
        sh '''
        echo 'hh'
          // docker --version
          // docker compose version
        '''
      }
    }
  }
}